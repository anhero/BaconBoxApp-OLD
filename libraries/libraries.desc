#!/bin/bash
#Libraries to build

# If baconbox is not available directly in libraries, we build it.
if [[ ! -e baconbox ]]; then
	libraries=( ${libraries[@]-} 'baconbox' )
fi

libraries=( ${libraries[@]-} 'sigly' )

